<!DOCTYPE html>
<html lang="en">
    <%- include('_partials/_head', {title: 'List of Services'}) %>
<body>
    <%- include('_partials/_navbar', {heading: 'List of Services', user}) %>

    <% if (user.role === 'hacklancer') { %>  
        <button onclick="location.href='/services/create'">Create New Service</button>
    <% } %>  

    <table>
        <tr>
            <th>No</th>
            <th>Title</th>
            <th>Description</th>
            <th>Price</th>
            <th>Terms</th>
            <th>Hacklancer</th>
            <th>Skill</th>
        </tr>

        <% services.forEach((service, i) => { %>
            <tr>
                <td><%= i + 1 %></td>
                <td><%= service.title %></td>
                <td><%= service.description %></td>
                <td><%= formatToCurrency(service.price) %></td>
                <td><%= service.termsDay %></td>
                <td>
                    <a href="/hacklancer/profile/<%= service.Hacklancer.id %>">
                        <%= service.Hacklancer.username %>
                    </a>
                </td>
                <td><%= service.Skill.name %></td>
            </tr>
        <% }) %>
    </table>
</body>
</html>